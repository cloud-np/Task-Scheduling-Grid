# Radnom Ruin
for task in tasks:
    if randint() > 0.5:
        Ruin Task's machine
        ruined_tasks.append(task)

# Random Recreate
for task in tasks:
    if task in ruined_tasks:
        Schedule Task to the next best machine.

#######################################################

# Time Ruin
Dertimine a time range to ruin.
for task in tasks:
    if window_start < task.start < window_end:
        Remove task from workflow.
        ruined_tasks.append(task)

# Time Recreate
for task in tasks:
    if task in ruined_tasks:
        Schedule Task to the 

#######################################################

# Comp/com Ruin
Sort tasks by computational/cummunication cost.
Ruin the 10% of the tasks with the highest computational/cummunication cost.

# Comp/com Recreate
for task in tasks:
    if task in ruined_tasks:
        Schedule Task to the next best machine.